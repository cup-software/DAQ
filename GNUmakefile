# SoftRelTools GNUmakefile for MySim top level

# Compile the files in src subdirectory
SUBDIRS=src dict test scripts

# Header files go in include subdirectory
PACKAGE_INCLUDE=DAQ

################################
include SoftRelTools/standard.mk

